# v0.5 来源与开源项目可行性报告

状态：智联核心来源迁移与 live smoke 完成
日期：2026-07-22
关联需求：`docs/03_requirements/v0.5-role-profile-graph.md`  
来源清单：`third_party/reference_sources/manifest.yaml`

## 1. 目的

在实现 v0.5 SourceAdapter 前确认：

1. 首版实际覆盖哪些第三方招聘、企业官网和经验来源；
2. 哪些 GitHub 项目可以包装、只能参考或必须拒绝；
3. 企业官网通用采集的确定性能力能覆盖到哪里；
4. 登录态来源可行性与实现后 adapter 验收之间的边界。

本报告不把“仓库能下载/编译”或“浏览器页面可访问”视为“adapter 已实现”。开源项目
只有同时通过许可证、安全边界、结构化输出、raw-before-parse 适配和最小 adapter
live smoke，才可进入 `adopt`；P1 浏览器来源 smoke 只解除代码实现前的来源不确定性。

## 2. 首版平台范围

| 层级 | 平台/来源 | v0.5 决策 | 说明 |
| --- | --- | --- | --- |
| 第三方岗位发现 | 智联招聘 | 核心，adapter live smoke 通过 | 使用只读、单页、raw-first 的 `zhaopin_jobs`；真实归档重放解析 20 条 |
| 第三方岗位发现 | BOSS 直聘 | 退出运行时 | 历史 P1 可访问，但连续触发严格风控；不再继续尝试或保留运行时代码 |
| 第三方岗位发现 | 前程无忧/51job | backlog | 当前参考项目仅注册占位，不作为首版依赖 |
| 岗位核验 | 企业官网/招聘站 | v0.5 必需 | 每个保留 cluster 形成明确 verification status |
| 岗位核验 | 常见 ATS | adapter backlog | 优先复用公开结构化接口/JSON-LD，不承诺覆盖所有中国企业 |
| 经验来源 | 牛客 | 首选，P1 浏览器 smoke 通过 | 直接采集项目成熟度不足，倾向自建有界 adapter |
| 经验来源 | 小红书 | v0.5 非必需候选 | 登录、许可证和运行环境成本较高，先保留接口与参考代码 |
| 经验来源 | 知乎 | fallback backlog | 后续按岗位方向补充 |
| 经验来源 | 脉脉、看准 | postponed | 登录、个人信息和来源政策风险较高，不进入 v0.5 |

首版真实链路因此固定为：

```text
zhaopin_jobs discovery
  → third-party raw evidence
  → deduplicated JobPostingCluster
  → official_careers verification
  → official raw evidence
  → JobIdentityLink
  → FieldResolution
  → Role Claim / RoleProfile

nowcoder_experience
  → experience raw evidence
  → scoped hiring signals
```

## 3. 准入规则

每个上游项目检查以下字段：

- repository、固定 commit、license；
- 最近维护状态、测试和可运行入口；
- 是否要求登录、如何保存 Cookie/Token；
- 是否默认绕过验证码、robots、限流或风控；
- 是否输出稳定 JSON/错误语义；
- 是否能在解析前保存 raw response；
- 是否可以放在本项目 SourceAdapter 后方；
- offline smoke 和 live smoke 结果。

状态定义：

- `adopt`：允许作为 adapter 依赖候选；
- `blocked_by_auth`：静态/离线检查通过，等待正常登录 live smoke；
- `reference_only`：只学习架构/解析方式，不复制或直接依赖；
- `rejected`：违反许可证、安全、维护或证据边界。

开源许可证只决定代码使用权限，不等于目标网站授权采集。

## 4. 项目检查结果

| 项目 | 用途 | License | P0 结果 | 决策 |
| --- | --- | --- | --- | --- |
| [boss-zhipin-scraper](https://github.com/eatmoreduck/boss-zhipin-scraper) | BOSS CDP 职位发现 | MIT | v2.1.0 隔离安装、CLI 和 76 项测试通过；真实 CDP 探测返回 `code 37` 并停止；缺少 raw-before-parse，且包含 visibility override 与模拟用户行为 | `rejected` |
| [boss-agent-cli](https://github.com/can4hou6joeng4/boss-agent-cli) | 历史 BOSS/智联参考 | MIT | 安装成功；3 个 schema contract 测试通过；但 BOSS 已退出运行时，智联采用本项目 raw-first adapter | `rejected` |
| [OfferClaw](https://github.com/InuyashaYang/offerclaw) | 牛客/小红书经验参考 | 未声明 | Python 编译、离线去重通过；公开搜索 smoke 返回空；无正式测试；直接解析后输出且未先归档 raw；用估算值代替拿不到的点赞数 | `reference_only` |
| [Agent-Reach](https://github.com/Panniantong/Agent-Reach) | 多后端路由/doctor 参考 | MIT | Python 编译通过；首选/备用后端和健康探测设计可复用 | `reference_only` |
| [MediaCrawler](https://github.com/NanmiCoder/MediaCrawler) | 小红书采集参考 | Non-Commercial Learning 1.1 | Python 编译通过；许可证限制非商业学习和大规模采集 | `reference_only` |
| [xiaohongshu-mcp](https://github.com/xpzouying/xiaohongshu-mcp) | 小红书 MCP 候选 | 未声明 | 仓库已保存；当前环境缺少 Go，未构建；需要手动登录 | `reference_only` |
| [Ever Jobs](https://github.com/ever-jobs/ever-jobs) | ATS registry/JSON-LD 参考 | MIT | 包含通用 `JobPosting` JSON-LD parser、测试和大量 ATS adapter；主要面向海外来源且为大型 TypeScript monorepo | `reference_only` |
| [Crawl4AI](https://github.com/unclecode/crawl4ai) | 通用 fetch/render 候选 | Apache-2.0 | Python 编译通过；基础依赖包含 Playwright、Patchright、stealth 和 fake user-agent，超出 v0.5 默认 transport 的最小边界 | `reference_only` |
| [Trafilatura](https://github.com/adbar/trafilatura) | HTML 正文 fallback | Apache-2.0 | 安装和离线抽取通过；小型 JD 页面仍保留导航噪声 | `candidate` |
| [Extruct](https://github.com/scrapinghub/extruct) | JSON-LD/Microdata | BSD-3-Clause | 安装成功；离线 `schema.org/JobPosting` 正确提取岗位标题与城市 | `adopt` |

固定 commit、完整备注见 manifest。

## 5. 关键发现

### 5.0 2026-07-22 新候选 `boss-zhipin-scraper`

对固定版本 `d150a2cea945c5c40966d5c65f41b68b8b7ef46d` 的补充评估显示，上游
v2.1.0 可安装、两个 CLI 入口可运行、76 个离线测试全部通过。真实 CDP 环境能够连接
Chrome 150，但第一次 BOSS 搜索探测返回 `code 37 / 您的环境存在异常`；上游正确将其
分类为 restricted 并立即停止，本轮未继续运行正式 smoke 或抓取。

该项目不进入运行时依赖：搜索响应在页面 JavaScript 内先解析和字段投影，未先归档完整
raw response；同时包含后台 visibility override、模拟滚动和鼠标移动等行为，不符合本项目
安全边界。完整记录见 `v0.5-boss-zhipin-scraper-evaluation.md`。

### 5.1 智联替代 BOSS 成为核心招聘发现来源

2026-07-22 已删除 `BossJobsAdapter`、BOSS parser 和 BOSS CLI credential 入口。
`zhaopin_jobs` 复用旧 recruitment-crawler Skill 的搜索 URL、卡片字段和登录页识别经验，
但重新接入本项目的 raw-before-parse、SourceBatch、credential ref、域名白名单和低频边界。

真实单页 smoke `v05-live-zhaopin-20260722-b` 成功归档 1 个 HTML 响应；从该 raw Artifact
重放得到 20 条规范化岗位，其中 19 条满足成都 hard scope。首次分类曾把页面内普通登录
链接误判为登录墙，已通过“有效岗位结构优先、最终 URL/无岗位验证页才中断”的测试修复。

BOSS 的历史测试与 rejected 候选记录继续保留为决策证据，但不再是当前来源或待办项。

### 5.2 现有牛客项目不适合直接并入

本轮找到的传统牛客爬虫大多长期未维护。较新的 OfferClaw：

- 没有许可证；
- 通过搜狗发现牛客 URL，本次查询返回空结果；
- 没有 raw-before-parse；
- 页面拿不到点赞数时会生成“质量代理值”，这在统一证据层中属于伪造字段；
- 无法提供 SourceRunReceipt、授权状态和可重放 raw。

因此只能参考 selector/查询词，`nowcoder_experience` 应由本项目按正常登录、
Copy as cURL、raw archive 和 strict JSON contract 自建。

### 5.3 “通用官网爬虫”必须拆成分层能力

离线测试证明：

- Extruct 可以稳定完成标准 JSON-LD 提取；
- Trafilatura 可以抽取正文，但不能保证去除所有导航噪声；
- Ever Jobs 证明 ATS registry 与 JSON-LD-first 具有工程可行性；
- Crawl4AI 能力广，但浏览器、stealth 和依赖范围过重，不适合作为 v0.5 默认基础。

因此 `official_careers` 不采用“一个通用爬虫覆盖所有网站”，而采用：

```text
bounded HTTP fetch + immutable raw archive
  → JSON-LD JobPosting
  → registered ATS/site adapter
  → deterministic DOM/text extraction
  → LLM strict JSON extraction
  → adapter_required
```

最后的 `adapter_required` 是合法结果。LLM 不生成并执行新爬虫代码。

### 5.4 MCP 是可选 transport，不是证据层

上游 CLI、MCP 或 Python 库都只能位于 SourceAdapter 后方。无论底层使用哪种 transport：

- raw bytes 必须先进入本项目 Evidence Store；
- ToolResult 只返回本项目 schema；
- credential 只能以 ref 进入 Graph；
- 上游项目不能直接写画像、checkpoint 或业务数据库。

## 6. P0 Smoke 记录

| 项目 | 检查 | 结果 |
| --- | --- | --- |
| zhaopin_jobs | 真实单页 raw-first smoke | Passed；1 response / 1 archived artifact |
| zhaopin_jobs | 真实 raw 重放解析 | 20 条规范化岗位，19 条满足 hard scope |
| boss-zhipin-scraper | 隔离安装、语法、CLI | 通过，v2.1.0 |
| boss-zhipin-scraper | 上游离线测试 | `76 passed` |
| boss-zhipin-scraper | 真实 CDP 环境检查 | Chrome 连通；搜索探测 `code 37`，正确停止；live 未通过 |
| boss-agent-cli | Python 安装 | 通过 |
| boss-agent-cli | 3 个 schema contract tests | `3 passed` |
| boss-agent-cli | `schema --format openai-tools` | 通过，输出 JSON envelope |
| boss-agent-cli | 无登录 `status` | 正确返回 `AUTH_REQUIRED` |
| BOSS 网页 | 当前内置浏览器登录态 | 首页可访问但未登录，不计作 live smoke |
| OfferClaw | Python compile | 通过 |
| OfferClaw | 离线重复内容去重 | 通过 |
| OfferClaw | 最多 3 条公开搜索 | 返回空结果 |
| Crawl4AI | Python compile | 通过 |
| Agent-Reach | Python compile | 通过 |
| MediaCrawler | Python compile | 通过 |
| xiaohongshu-mcp | Go test | 未执行，当前环境缺少 Go |
| Extruct | JobPosting JSON-LD | 通过，提取标题和城市 |
| Trafilatura | JD 正文提取 | 基本通过，但包含导航噪声 |

## 7. P1 浏览器来源 Smoke（历史）

完整的无凭据运行收据见
`docs/07_evaluation/v0.5-p1-browser-smoke-report.md`。

| run | 来源与查询 | 结果 | 判定 |
| --- | --- | --- | --- |
| `P1-BOSS-20260719-AI-AGENT` | BOSS：`AI Agent` 搜索与京东科技岗位详情 | 能读取状态、薪资、学历、职责、要求、地点和更新时间 | Passed |
| `P1-NOWCODER-20260719-JD-AI-AGENT` | 牛客：`京东 AI Agent 面经` 搜索与详情 | 返回多条相关面经，详情含后端与 Agent 面试信号 | Passed |
| `P1-OFFICIAL-20260719-JD` | BOSS 京东科技候选 → 京东校招官网：`AI Agent` | 官网当前无对应岗位，保存为 `official_not_found` | Passed |

本轮只使用用户正常登录的 Chrome；未读取或导出 Cookie、密码、Token、浏览器存储或
Copy as cURL，也没有投递、沟通、点赞、评论或收藏。

### 7.1 官网核验案例

BOSS 候选岗位为京东科技 `ai agent开发`，面向 2026 届，BOSS 页面显示“招聘中”，
页面更新时间为 2026-06-19。京东校招官网显示 2027 校招正在进行，但用 `AI Agent`
检索没有对应岗位。

因此：

- 官网能够确认企业招聘站和当前校招批次，但不能确认同一岗位；
- 核验状态为 `official_not_found`，不生成 confirmed identity link；
- BOSS 记录保留，不能仅因官网未找到而判定虚假；
- 画像必须显示来源时间、毕业批次和官网核验状态。

### 7.2 P1 与实现后 live smoke 的边界

P1 曾证明三类来源在正常登录和公开官网条件下可访问、可搜索、可读取和可核验。
2026-07-22 的来源切换后，BOSS P1 仅作为历史证据，不计入当前 adapter 验收。
它没有证明尚未实现的 SourceAdapter 已满足 raw-before-parse。

代码完成后仍需：

1. 三个 adapter 分别执行 opt-in live smoke；
2. 原始响应在解析前进入 Evidence Store；
3. 生成不含凭据的 SourceRunReceipt；
4. 验证 credential ref、auth interrupt/resume、分页、限流、幂等和脱敏；
5. 运行至少一个自动化官网核验案例。

## 8. 当前结论

最新架构已经通过实现前来源门禁：

- 文档架构：通过；
- 开源候选下载与 P0 静态/离线评估：通过；
- 企业官网确定性解析方向：通过，明确接受 `adapter_required`；
- 智联 adapter 单页 raw-first smoke 与真实页面重放：通过；
- 牛客浏览器来源 smoke：通过；
- 第三方岗位到企业官网的真实二次核验：通过，得到预期的 `official_not_found`。

因此 v0.5 招聘发现核心已切换为智联：`zhaopin_jobs` 的代码、离线测试、真实单页传输和
raw 重放均通过。BOSS 相关上游仅保留历史准入结论，不进入运行时。版本完全 Implemented
仍需至少一条真实 `智联候选 → 企业官网 confirmed identity link → FieldResolution`。
