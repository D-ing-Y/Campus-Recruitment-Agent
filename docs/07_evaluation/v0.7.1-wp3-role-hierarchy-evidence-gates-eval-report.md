# v0.7.1 WP3 岗位层级与证据门禁 Eval Report

状态：Offline Passed / Authenticated-live Pending
日期：2026-08-03

## 1. 本次解决的问题

- 多个求职方向不再共用第一个 family，而是分别生成 SearchScope 和 Handoff。
- 搜索噪声不再因继承 SearchScope family 而进入岗位族统计。
- 搜索卡片不再被当作完整 JD 生成 JobInstanceRoleProfile。
- 社区经验必须先确定 family/company/job scope，无法唯一归属时不投影。
- 新线程重放时已有 official verification plan 的 ID 会重新装入 State，不会因“已存在所以跳过”丢失后续验证链。

## 2. 验证结果

| 指标 | 结果 | 证据 |
| --- | ---: | --- |
| `cross_family_scope_mix_count` | 0 | multi-family scope + cross-family noise integration |
| `unclassified_job_projection_count` | 0 | membership accepted-only clustering |
| `search_only_job_profile_count` | 0 | search-only Graph case |
| `detail_artifact_trace_rate` | 1.0 | 3/3 eligible fixture clusters 具有 detail document/artifact receipt |
| `ambiguous_experience_projection_count` | 0 | duplicate job-instance candidate unit case |
| `confirmed_experience_scope_trace_rate` | 1.0 | family/company/job scope-link tests |
| `duplicate_gate_record_count` | 0 | cross-thread fixture replay gate record counts |

## 3. 执行证据

- 聚焦验证：Role gate/Role Graph/CareerIntent/Matching 测试通过。
- 全量回归：`466 passed in 174.90s`。
- 静态验证：`compileall` 与 `git diff --check` 通过。
- WP2 兼容：单 family 仍提供 singular scope/handoff；多 family 使用 plural refs；
  Matching intent revision 传递 `requested_scopes`。

## 4. 边界与 Gate Decision

本报告只证明四个层级/证据门禁已实现并通过离线重放。当前 live adapter 尚未实现
完整的 search → detail 拉取和正确 document kind 分类，因此 WP3 Role live source 仍为
`not_started/partial`，不得声称 authenticated-live 通过。
