# v0.7.1 WP1.3.1 Resume Fidelity Correction Eval Report

状态：Implementation Passed / Human Review Pending  
日期：2026-07-31

## 1. 修复结果

- Resume 主解析器改为 `pypdf_layout`，教育记录与研究方向/核心课程归属恢复正确；
- 无标签头部的出生年月和籍贯由本地解析补齐，不进入 LLM；
- 项目/教育中的“至今”保留为原始证据值；
- 每个非空叶子字段必须存在精确 Fragment span，删除整页 SourceRef fallback；
- Source 摘要按当前 record/block span 生成；
- 非空列表最后一条完成后自动确认，空列表仍需 `confirmed_empty`；
- `--reparse` 生成新 Draft，旧 Snapshot 不变，确认后发布 version+1。

## 2. 自动化证据

- 全量：444 passed；
- `compileall` 和 `git diff --check` 通过；
- 迁移在真实数据库副本通过，旧 Draft、14 条 Receipt、旧 Snapshot 保留，
  `foreign_key_check=0`。

## 3. 真实 DeepSeek / PDF 证据

- Real provider run：`run-ceb5ff01-4ab0-4dc6-9dd7-e3f6bf6a0b8a`，cache miss、0 retry、
  Tool Calling + Pydantic accepted；
- Current review run：`run-82482f23-7a67-4545-b476-e98c82c6935f`；
- Current Draft：`resume-draft-6d718b55-ade8-406b-97ab-3d8df888ae67`；
- 结构核对：个人信息必需值完整；2 条项目；2 条教育；2 条奖项；专业技能完整；
  PDF 中缺失的优势、期望职位、工作经历保持空；
- 36 个非空叶子字段、36 个精确 SourceRef，缺失 span 为 0；
- 当前项目 Source 摘要命中项目标题且不从页首开始；
- `pre_confirmation_claim_count=0`、`pii_leak_count=0`、未发布新 Snapshot。

## 4. 边界

提取内容和代码验收已达标。当前 Draft 停在 `personal_information` 审核点；用户完成
确认后才发布新 Snapshot，之后才使用新 CandidateSnapshot 重验 WP2。
