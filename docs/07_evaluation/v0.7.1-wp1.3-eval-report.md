# v0.7.1 WP1.3 Structured Resume Evidence Eval Report

状态：Implementation Passed / E4 Human Review Pending  
日期：2026-07-31

> 该报告的首轮 Draft 忠实度结论已被 WP1.3.1 纠错报告替代；旧 Snapshot 仅作历史证据，
> 不用于新 CandidateProfile。

## 1. 结论

WP1.3 的代码、匿名回归、installed CLI、跨进程恢复、真实 DeepSeek 提取和隐私门禁已通过。
真实简历已经生成结构化 Draft，并在第一个 `personal_information` 审核点中断；在用户依次确认全部
区块以前，不发布 ResumeEvidenceSnapshot，不生成新的 Candidate Claim，也不恢复 WP1/WP2 Passed。

## 2. 自动化结果

- Unit：320 passed；
- 非 CLI Integration：66 passed；
- installed Candidate/Resume CLI：4 passed；
- CLI foundation/model UI/Intent CLI：11 passed；
- Eval fixtures：38 passed；
- 合计：439 passed；`compileall` 与 `git diff --check` 通过；
- 覆盖固定顺序、confirmed_empty、record/section review、retry、cancel、错误 identity/request、
  response idempotency、CAS、扫描 PDF 拒绝、pypdf/pdfplumber 门禁、PII 脱敏、旧 `--input` 拒绝及
  confirmed snapshot typed handoff。

## 3. 真实 DeepSeek E4 阶段证据

- Session：`session-fd8ce1d5-9a53-4877-b646-b6d89edb2b0e`；
- Run：`run-13496fb4-f439-411e-be4d-73e7c9a34f7c`；
- Provider：LangChain DeepSeek integration，`effective_strategy=tool_calling`，cache miss，0 retry，
  Pydantic accepted；
- Draft：`resume-draft-b51904fa-985d-49e3-bca5-7a80c90f51bd`；
- 状态：`awaiting_review`，当前请求为 `personal_information`；
- 解析器：pypdf，质量门禁通过；31 个非空结构化叶子字段，缺失 SourceRef 数为 0；
- 模型提取出 2 条项目经历、2 条教育经历、2 条自定义记录和专业技能块；工作经历、期望职位为空；
- run artifact 对已识别 PII 和原 PDF 文件名的扫描命中均为 0；ResumeEvidenceGraph 当前运行没有
  Candidate Claim 写入。

本报告不记录真实字段值、完整 Prompt、简历正文或 API key。

## 4. 未完成门禁

1. 用户按 BOSS 顺序确认、纠正或删除所有记录；
2. 发布 confirmed ResumeEvidenceSnapshot；
3. 使用该 Snapshot 构建新的 CandidateSnapshot，并验证所有简历派生 Claim 的
   `source_evidence_ids + evidence_fragment_ids`；
4. 使用新 CandidateSnapshot 重跑 WP2 CareerIntent。

完成以上四项后，才把 WP1.3、WP1 和 WP2 恢复为 Passed。
