# v0.7.1 WP3.1 Eval Plan：岗位需求与评价画像分流

状态：WP3.1.2 Offline Passed（L1 live accepted，multi-platform L2 partial）
日期：2026-08-11

## 1. 目标

验证新 Workflow 同时解决两个问题：默认岗位闭环不再依赖逐公司官网 adapter；不同用途的社区证据不会
污染岗位要求、备考计划或岗位/公司评价。Eval 必须区分 contract、fixture、integration 与 live 证据层级。

## 2. 核心指标

| Metric | Acceptance |
| --- | ---: |
| `mandatory_official_verification_count` | 0 |
| `platform_detail_to_demand_trace_rate` | 1.0 |
| `search_only_demand_profile_count` | 0 |
| `interview_segment_reputation_leak_count` | 0 |
| `employment_segment_demand_leak_count` | 0 |
| `unknown_or_ambiguous_segment_projection_count` | 0 |
| `mixed_segment_quote_trace_rate` | 1.0 |
| `company_only_to_job_reputation_leak_count` | 0 |
| `reputation_dimension_sample_trace_rate` | 1.0 |
| `consumer_input_scope_violation_count` | 0 |
| `duplicate_segment_or_profile_write_count` | 0 |
| `checkpoint_extracted_body_count` | 0 |
| `checkpoint_tool_result_max_bytes` | <= 50000 |

`mandatory_official_verification_count=0` 不表示 official adapter 被删除；条件触发的 escalation 单独统计。

## 3. Contract 与单元 Eval

### E1 招聘来源门禁

- search card + 无 detail：不得创建 Demand；
- recruitment detail + 完整 identity：允许创建 Demand；
- official 不可用：默认路径仍可完成；
- 关键字段跨平台冲突：创建 OfficialEscalationReceipt；
- 疑似关闭或用户显式要求：创建 escalation，但失败状态必须保留。

### E2 社区文档分类

覆盖 interview、employment、mixed、unknown：

- mixed 必须拆出独立 quote/Fragment；
- unknown、无详情、无 quote 和 scope ambiguous 不投影；
- LLM 输出非法 segment_type/usage 时 Validator 拒绝。

### E3 使用权限

- interview_question -> demand_assessment：接受；
- interview_question -> reputation：拒绝；
- work_intensity -> reputation_job/company：按 scope 接受；
- work_intensity -> Demand/Preparation requirement：拒绝；
- company-only evidence -> JobReputationProfile：拒绝。

### E4 聚合语义

- 单篇评价只标记 insufficient_sample/observed；
- 多个同向独立来源可以提升 sample status；
- 相反评价保留 disputed 与双方引用；
- 公司聚合保留不同 role family 的样本分布；
- 任何输出均不存在 overall score。

## 4. Graph 与恢复 Eval

- 多 SearchScope 分别建立 CompanyRoleGroup，不跨 scope 污染；
- recruitment 和 community 预算分别耗尽时保留各自阶段状态；
- community 被阻断时允许发布 Demand，并在 Bundle 标记 Reputation 缺失；
- Demand 缺少平台 detail 时不得用社区内容代替；
- 重复运行、重复详情和重复 resume response 零重复写入；
- 旧 RoleProfileSnapshot 可读，新 run 不回写旧 `hiring_signals`。

## 5. Consumer Isolation Eval

- Matching 输入只含 accepted Demand requirement IDs；
- Preparation 输入只含 requirement 和 assessment signal IDs；
- Reputation 的 work intensity、atmosphere、compensation 等 ID 不进入能力 Gap 或学习 Activity；
- TargetDecision 与 Role Q&A 可以读取 Reputation，但输出标注来源类型；
- consumer 收到越界 ID 时返回 `evidence_usage_violation`。

## 6. Fixture 场景

至少覆盖：

1. 单公司单岗位 + 招聘详情 + 面经；
2. 单公司多个相似岗位 + 公司级评价；
3. 单篇 mixed 帖子同时包含面试和入职体验；
4. 平台之间职责或地点冲突；
5. 只有 search card、没有 detail；
6. 社区搜索命中但详情需要登录；
7. 多篇评价对工作强度结论冲突；
8. generic family 面经与 company-role 面经同时存在。

## 7. 受控 Live Eval

### L1 招聘平台闭环

使用至少一个真实启用 adapter 完成：

`SearchScope -> search Raw -> platform job-detail Raw -> JobDemandProfile`

记录 source、artifact/document/profile ID、安全统计和失败码；不提交完整 JD 或个人化查询正文。

### L2 社区闭环

使用至少一个真实启用 adapter 完成：

`CompanyRoleGroup -> community search Raw -> post-detail Raw -> typed segment -> profile`

至少各验证一条 interview 和 employment segment；若来源受登录、风控或空结果限制，状态为 blocked/partial，
不得用 fixture 替代 live accepted。

## 8. WP3.1 完成条件

- 所有核心指标通过；
- 聚焦与全量回归通过；
- L1 和 L2 均有真实 detail Raw Artifact 与投影追溯；
- 版本矩阵明确区分 offline fixture 与 authenticated/live 结果；
- Demand、Reputation、Bundle 的当前对象 ID 和安全统计写入执行日志；
- 未满足时 WP3.1 保持 `Ready for Implementation`、`In Progress` 或 `Blocked`，不得标记完成。

## 9. 2026-08-06 实际结果

证据层级必须分开读取：

| 层级 | 结果 | 安全证据 |
| --- | --- | --- |
| Contract / unit | passed | SourceDetailRequest、candidate-only discovery、quote 唯一定位、usage/scope allowlist、官方升级策略 |
| Offline graph / CLI | passed | 33 项 WP3.1 聚焦测试；最终全量 484 passed / 1 live skipped；含 detail gate、mixed segment、跨进程恢复、重复 response、skip/cancel、多 handoff |
| Live L1 recruitment | accepted | 3 个真实 `job_detail` Raw Artifact，3 个 JobDemandProfile，1 个 RoleFamilyDemandProfile |
| Authenticated live community search | passed | 牛客“公司+岗位+面经”与“公司+岗位+工作体验”两类搜索均完成 Raw 归档 |
| Live L2 community detail | partial | 两类真实搜索均未发现可接受的帖子详情候选，因此 0 Segment；没有使用搜索摘要、Fixture 或模型常识替代 |

L1 对象（只记录 ID，不记录完整 JD）：

- SourceDocument：`d087784b-a2d9-5176-8a18-07bb7f015ec1`、
  `4dcc54a5-dfd5-58db-b934-db36a92170b6`、`d61061be-8db0-5e12-88f5-bcd20eaeb170`；
- JobDemandProfile：`job-demand:0110ed38ade6ac8be9d98978`、
  `job-demand:e152090e8140f29dd09cd44f`、`job-demand:db94f5c3241a726f5215726c`；
- RoleFamilyDemandProfile：`family-demand:62b0f5c557166905871e4cf8`；
- RoleIntelligenceBundle：`role-intelligence-bundle:96e7107270407b42d9532a1d`。

安全统计：

```text
search_only_projection_count = 0
detail_artifact_trace_rate = 1.0
job_demand_profile_count = 3
role_family_demand_profile_count = 1
interview_segment_reputation_leak_count = 0
employment_segment_demand_leak_count = 0
community_segment_count = 0
new_checkpoint_regression_extracted_body_count = 0
new_checkpoint_regression_tool_result_max_bytes < 50000
```

当前 Bundle 明确标记 `interview_assessment`、`job_reputation`、`company_reputation` 缺失。
因此代码闭环和 L1 已验收，但 L2 尚未满足“一条面经 Segment + 一条工作体验 Segment”的完成条件，
WP3.1 不能标记为完整 live accepted。

真实 Session 恢复还暴露了 checkpoint 边界缺口：旧实现将包含详情页正文的
`ToolResult.records` 按轮追加，造成本地 Role checkpoint 异常膨胀。现已改为有界摘要覆盖、
授权恢复及最终化清空，并增加正文不进入 checkpoint 的单元与 Graph 回归。历史文件
保留作为运行审计，未做破坏性改写。

对当前 WP3 handoff 的公开 CLI 恢复验证为：

- Session：`session-772ebdf5-79aa-47a2-a9e9-1b5433c82506`，version 15，当前阶段 `matching`；
- resolved handoff：`handoff:edabd05ea040767b9fe4ef0a`；
- Demand-only Bundle：`role-intelligence-bundle:7c2658bd38e72b9abd35c8b7`；
- 20 个 JobDemandProfile、0 个 ReputationProfile，`detail_artifact_trace_rate=1.0`、
  `search_only_projection_count=0`；
- 评价来源跳过后正确返回 `completed_with_unknowns -> match.run`，而不伪造完整 Bundle。

最终自动验证：`484 passed, 1 skipped in 111.22s`；默认 live 分组单独运行为
`1 skipped`；`compileall`、`git diff --check` 和无 `PYTHONPATH` 的安装态 `role --help`/
`model list` 黑盒冒烟通过。因原 `.venv` 位于 iCloud Documents 且依赖文件被按需卸载，
最终全量回归在 `/private/tmp` 干净环境中按同一 `pyproject.toml` 约束安装后执行。

## 10. WP3.1.1 补丁验收

### E5 确定性查询循环

- round 1/2/3 严格对应完整岗位、岗位族展示名、公司；内部 family ID 不进入查询。
- 任一轮达到两篇独立合格详情后不再调用后续轮次或备用来源。
- 面经按牛客到小红书、工作体验按小红书到牛客切换；单来源单用途不超过三轮。
- 两个来源不足或全局预算耗尽时 Bundle 显式 partial/blocked。
- 重复 URL、平台帖子 ID、正文 hash 在跨轮/跨平台转载时只计一次。

### E6 Sidecar 与敏感信息

- 假 Sidecar 验证 health、auth、search、detail、超时、单任务隔离和旧输出排除。
- 非 localhost、未确认许可证、未固定 commit、越界输出路径和禁止的采集能力均被拒绝。
- Cookie、xsec_token、签名参数、输出路径和正文不进入 checkpoint、日志或报告。
- 搜索原始响应先归档，但 `search_only_projection_count=0`。

### L3 多平台真实验收

- 牛客分层放宽能发现并归档真实帖子详情，或以各轮空结果/阻断回执结束。
- 小红书使用正常登录的 Chrome/CDP 会话完成 search、detail 和 Raw Artifact 归档；不绕过验证码或风控。
- 面经与工作体验各至少两篇独立合格详情；所有 Segment 追溯到详情 Artifact、Fragment 和字符范围。
- 样本不足、Sidecar 未安装、登录失效或风控时保持 partial/blocked，Fixture 不计为 live accepted。

### 2026-08-11 离线实施结果

- 新增确定性 3 轮查询、两篇停止、主备切换、AttemptReceipt/Coverage 和跨轮去重；
- Graph fixture 验证第一轮两篇即停、三轮不足、跳过受阻主来源后切换备用来源；
- 详情 scope 必须由正文/标题中的公司、完整岗位或岗位族词确认，query group 不再提供证明；
- 假 Sidecar 验证只读 MCP allowlist、search/detail、旧输出隔离、localhost/commit/license/path 门禁，
  MCP 响应不暴露 xsec_token；
- 聚焦 41 项通过；全量结果见版本执行日志；compileall、`git diff --check` 和安装态 CLI 冒烟通过；
- 本轮未配置或运行真实 MediaCrawler/小红书会话，因此不得把 L3 写成 accepted；此前牛客 L2 partial
  结果也不因代码补丁自动升级。

## 11. WP3.1.2 真实协议与诊断验收

### E7 MediaCrawler 协议

- fake upstream 必须复刻固定 commit 的 `/api/crawler/status`、`/api/crawler/start`、
  `/api/data/files` 和 `preview=false` 文件下载，不接受测试专用路径。
- 验证同日 append 文件只返回本轮关键词/post ID 对应内容，旧文件和旧记录均不得进入候选。
- MCP stdio smoke 必须列出四个只读 Tool，并证明 Cookie、xsec_token、签名、文件路径不出站。

### E8 公司别名与牛客诊断

- 法定名到品牌名只允许命中版本化 allowlist；未知别名不得由模型自动确认。
- 别名查询命中后，详情正文仍必须确认法定名或已核验品牌名以及岗位/岗位族 scope。
- 使用旧牛客 Raw 回放，结果必须为 `non_post_cards_only` 而非 `parser_changed`；构造含未识别帖子
  结构的 fixture 时才允许 `parser_changed`。

### L4 真实执行

- 使用现有 pending SearchScope，牛客逐轮留下 Raw、AttemptReceipt 和 Diagnostic。
- 在仓库外固定 MediaCrawler commit，通过正常 Chrome CDP 或可见浏览器登录小红书，执行 MCP 与
  adapter search/detail。
- 面经和工作体验各两篇独立合格详情才标记 L2 accepted；否则按来源和用途记录 partial/blocked。
- `search_only_projection_count=0`、`detail_artifact_trace_rate=1.0`、两类用途串线数为 0，所有 Segment
  可追溯到 detail Artifact/Fragment/字符范围，敏感信息泄漏数为 0。

## 12. 2026-08-12 WP3.1.2 实际结果

| 层级 | 结果 | 安全证据 |
| --- | --- | --- |
| Contract / offline | passed | 真实 `/api` wire shape、本轮文件隔离、公司 allowlist、三层查询、诊断、Sidecar 失败分类与详情 scope 均有回归 |
| DeepSeek protocol | passed offline | 5 项 model gateway 测试证明 Tool Calling 且 `thinking=disabled`；本轮无合格社区详情，真实模型分类调用数为 0 |
| Recruitment L1 | accepted | 当前成都 Scope 生成 1 个 `job_detail` Raw、1 个 JobDemand、1 个 FamilyDemand 与 Demand-only Bundle |
| Nowcoder L2 | partial | 四轮成功搜索只得到非帖子卡，post candidate 为 0；后续查询返回 `authentication_required` |
| Xiaohongshu MCP | passed | 固定 commit 的 localhost Sidecar 与四个只读 stdio Tool 真实进程 smoke 通过 |
| Xiaohongshu content L2 | blocked | 可见 Chrome/CDP 导航超时；受控浏览器安全策略拒绝跨浏览器/CDP 绕行，因此未产生 search/detail Raw |
| Multi-platform L2 | partial | 0 个社区详情、0 个 Segment；未用搜索摘要、Fixture 或模型常识替代 |

本轮 L1 对象只记录 ID：

- SourceDocument：`71a35815-a06b-5344-9a09-1626ea26a6a4`；Raw Artifact：`d6c34389-b526-5900-9728-a6712b3def45`；
- JobDemandProfile：`job-demand:c29ab904839d3f097212a7fe`；
- RoleFamilyDemandProfile：`family-demand:4676936cc22871b39ef015af`；
- RoleIntelligenceBundle：`role-intelligence-bundle:e482a1ea6e908e1a5cdb5f40`。

安全统计：

```text
search_only_projection_count = 0
detail_artifact_trace_rate = 1.0
nowcoder_successful_search_diagnostic_count = 4
nowcoder_post_candidate_count = 0
nowcoder_non_post_record_count = 5
community_detail_document_count = 0
community_segment_count = 0
model_live_classification_call_count = 0
sensitive_locator_leak_count = 0
```

结论：WP3.1.2 代码和离线门禁完成，且修复后的目标城市详情预算已通过真实 L1；社区内容 L2 仍未满足，
所以 WP3 总状态保持 partial，不能进入“完整 live accepted”。下一次续验必须先刷新牛客登录态，并在
允许访问小红书的正常可见浏览器环境中重新执行，不得降低详情证据门禁。
