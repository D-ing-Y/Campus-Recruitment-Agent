# v0.7.1 WP2 CareerIntent Eval 计划

状态：Executed / Passed  
日期：2026-07-30

## 1. 指标

| 指标 | 门槛 |
| --- | ---: |
| raw_intent_evidence_trace_rate | 100% |
| structured_candidate_pydantic_pass_rate | 100% accepted output |
| confirmed_constraint_trace_rate | 100% |
| hard_vs_preference_classification_accuracy | 100% 关键边界集 |
| search_scope_projection_accuracy | 100% |
| same_scope_change_route_accuracy | 100% |
| scope_change_route_accuracy | 100% |
| duplicate_confirmation_write_count | 0 |
| unconfirmed_snapshot_publish_count | 0 |
| raw_or_secret_run_artifact_leak_count | 0 |

## 2. Evidence levels

- E0 schema/unit：枚举、canonical projection、一致性、hash。
- E1 deterministic fixture：关键中文边界集。
- E2 Graph：interrupt/resume、checkpoint、failure/idempotency。
- E3 installed CLI：任意 cwd create/revise/confirm/show/inspect。
- E4 DeepSeek：真实意图使用 non-thinking Tool Calling，Pydantic/领域验证通过。

## 3. 关键用例

- I0：Agent 开发 + 成都必须 + 2027 + 校招 + 大型/互联网科技优先。
- I1：`校招` 未明确季节，直接 confirm 被拒绝且零 snapshot。
- I2：revise 为 `autumn_campus` 后 confirm，发布 snapshot/scope/handoff。
- I3：模型把“优先”误分 hard 时 Validator 拒绝。
- I4：错误 fragment ref、wrong owner、stale request、不同 payload duplicate response。
- I5：LLM/storage/checkpoint 失败有 terminal run，raw-before-model 可检查。
- I6：同 response 重放零重复 snapshot/scope/handoff。

## 4. E4 真实输入

使用用户已确认的意图语义，不写入 Git：Agent 开发、成都 hard location、2027、校招范围待确认、
大型企业及互联网科技公司 negotiable。只提交 run/object/count refs，不提交 raw content/API key。

## 5. 放行

WP2 只有 E0-E4、全量回归、隐私扫描和人工复核全部通过后才能 Passed。成功只放行 WP3 开发，
不等同于 Role live source 或 v0.7.1 整体完成。
