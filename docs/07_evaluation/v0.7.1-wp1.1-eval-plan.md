# v0.7.1 WP1.1 LangChain 接入重构 Eval 计划

状态：Evaluated  
日期：2026-07-30
实际报告：`docs/07_evaluation/v0.7.1-wp1.1-eval-report.md`

## 1. 指标

| 指标 | 门槛 |
| --- | ---: |
| declared_vs_effective_strategy_accuracy | 100% |
| provider_native_mislabel_count | 0 |
| structured_schema_validation_rate | 100% accepted output |
| legacy_profile_resolution_rate | 100% fixtures/current local profile |
| model_visible_tool_schema_rate | 100% |
| implicit_tool_exposure_count | 0 |
| unauthorized_mcp_tool_load_count | 0 |
| tool_name_collision_accept_count | 0 |
| external_output_direct_profile_write_count | 0 |
| secret_private_content_leak_count | 0 |
| Candidate accepted_claim_projection_rate | 100% |
| Candidate silent_unprojected_active_claim_count | 0 |

## 2. Strategy matrix

| Case | capability | 预期 |
| --- | --- | --- |
| S0 | mock | mock structured response |
| S1 | native schema=true | provider_native_json_schema |
| S2 | native=false, tool=true | tool_calling |
| S3 | native/tool=false, json=true | json_mode + local validation |
| S4 | all false | unsupported_capability |
| S5 | explicit unsupported strategy | fail without silent fallback |
| S6 | declared native runtime error + allowed fallback | fallback receipt required |

## 3. Tool/MCP cases

- T0：旧 Tool 未附 spec，不能导出给模型；
- T1：Pydantic args schema 正常转换并调用原 ToolRegistry；
- T2：非法参数在副作用前拒绝；
- T3：write Tool 无确认策略时拒绝暴露；
- T4：本地与 MCP tool 重名时拒绝加载；
- M0：stdio fixture server allowlisted tool 可加载/调用；
- M1：未在 allowed_tools 中的 tool 不出现；
- M2：server 不可达形成 external_dependency，不影响其他 catalog；
- M3：配置/诊断/异常无 header、credential、API key 泄漏；
- M4：MCP 输出仅作为外部结果，不直接产生 Profile mutation。

## 4. DeepSeek 与 Candidate

- E3：active DeepSeek profile 由 LangChain integration 装配，health check 不发送业务材料；
- E4a：最小结构化 fixture 返回 `effective_strategy=tool_calling`；
- E4b：真实简历重新运行，accepted Claim、receipt、projection 指标全部通过；
- Replay：断网命中 strategy-aware cache，业务 snapshot/Claim ID 稳定；
- `deepseek-reasoner` capability fixture 不得错误选择 Tool Calling。

真实运行产生的 prompt/output、简历、key 与 MCP secret 不提交 Git；只记录 run/snapshot/统计 refs。

## 5. 放行

WP1.1 只有在 E0-E3、DeepSeek E4、Candidate 真实回归、全量测试和安全扫描全部通过后才能 Passed。
如果 MCP 仅 fixture 通过，结论只能是“协议接入已实现”，不能声称任意真实 MCP 服务已生产可用。
