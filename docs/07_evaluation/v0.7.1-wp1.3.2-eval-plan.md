# v0.7.1 WP1.3.2 Claim Lifecycle Eval Plan

状态：Passed（2026-08-02）；结果见 `v0.7.1-wp1.3.2-eval-report.md`。

## E0 Contract

- 新 Claim 来源、时间、lineage 与 legacy 兼容；
- 当前 Resume basis、overlay、HITL 和不可变 Snapshot 边界一致。

## E1 Unit

- origin/effective fallback 和多前驱校验；
- stale Resume、legacy model 隔离；
- capability 元数据等价、日期 refinement、真实冲突；
- 多前驱单事务 supersede、cycle/identity/idempotency。

## E2 Graph / CLI

- Resume v1 → v2 不混入 v1 Claim；
- conversation/feedback overlay 跨 Resume 保留；
- correction 只创建一个 successor；
- Provider 失败后 session.resume → candidate.build；
- current refs 驱动正确 next_action。

## E3 Regression

- Candidate、Feedback、WP2 聚焦测试；
- installed CLI 黑盒；
- 全量 pytest、compileall、git diff --check。

## E4 Real

保留当前错误 run，使用正式 cancel/resume/build 重建 Candidate。验证旧 Python、SQL 元数据和毕业时间
粒度不再产生伪冲突；真实跨来源差异仍进入 HITL。发布新 CandidateSnapshot 后，以它重跑 WP2 并
验证 CareerIntent/SearchScope/Handoff 的 typed input 证据。

## Exit

`stale_resume_claim_projection_count=0`、`legacy_model_claim_projection_count=0`、
`current_resume_claim_trace_rate=1.0`、`semantic_false_conflict_count=0`；E4 人工边界完成后才把
WP1/WP2 设为 Passed 并进入 WP3。
