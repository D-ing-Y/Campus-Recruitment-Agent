# v0.7.1 WP1.2 Candidate 经历 Taxonomy Eval Report

状态：Passed  
日期：2026-07-30

## 1. 结论

WP1.2 小补丁通过。原问题不是 DeepSeek Tool Calling 或 Pydantic 安全门失效，而是模型可见
Schema 未表达 Candidate 的固定值以及经历分类维度不足。当前模型边界已改为 Pydantic
discriminated union，经历使用 `kind + context + raw_label`，固定 capability id/level 和 experience
kind/context 均进入 Tool JSON Schema。

## 2. Schema 与确定性测试

- Tool Schema 包含 `claim_kind` discriminator，不再向新模型公开通用 `ExtractedClaim.value: Any`；
- 12 个 experience kind、18 个 context、5 个 capability level 和 8 个 versioned capability id
  均以 enum 暴露；
- 中文纵向/横向、课设/毕设、实习/工作项目、未知标签和 `proficient` 兼容归一化通过；
- capability `raw_label` 与 ontology ID 不一致时生成 `unmapped_capability_label` 或
  `capability_id_mismatch`，不再制造画像伪冲突；
- WP1/WP2 聚焦回归：41 passed in 48.00s。

## 3. 真实 DeepSeek E4

- session：`session-fdfc9715-d875-42bc-9851-acf2adbbe033`；
- run：`run-a0047b90-e893-4ced-8df8-314cdd0d3838`；
- Provider：`langchain_deepseek/deepseek-v4-flash`；
- `cache_hit=false`、`effective_strategy=tool_calling`、retry=0；
- Prompt/schema：`candidate_claim_extractor_v7` / `candidate_claim_v0.7.1.2`；
- 36 个模型项：18 accepted、18 rejected，全部有 ValidationReceipt；
- 两段真实经历分别投影为 `research + public_funded_research` 和
  `research + academic_research`，原始“项目经历/科研经历”与基金上下文均保留；
- Profile conflict=0，run status=completed，next action=`intent.create`；
- 归档、locator、receipt、predicate support、reference、projection、snapshot trace 指标均为 1.0，
  `silent_unprojected_active_claim_count=0`。

18 个拒绝项均为当前 ontology 未表示的 `skill_language/skill_tool`，拒绝原因是
`unsupported_predicate`。这是一条可诊断的契约边界，不是 Schema 解析失败或静默丢失。

## 4. 全量门禁

- `.venv/bin/python -m pytest -q`：428 passed in 85.47s；
- `.venv/bin/python -m compileall -q src tests`：passed；
- `git diff --check`：passed；
- WP2 代码和 CareerIntent contract 未修改。

## 5. 保留限制

当前 CandidateProfile 仍未正式投影荣誉奖项，以及未进入 CapabilityOntology 的语言、框架、库和
工具。它们的原始证据仍保留，模型项具有 rejected receipt；后续应通过独立的 qualification/
capability ontology 补丁扩展，不能在本次经历枚举补丁中强行映射。
