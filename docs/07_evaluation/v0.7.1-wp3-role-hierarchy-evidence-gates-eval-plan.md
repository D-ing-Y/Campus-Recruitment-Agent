# v0.7.1 WP3 岗位层级与详情证据门禁 Eval Plan

状态：Offline Executed（authenticated-live Pending）
日期：2026-08-03

## 1. 指标

| 指标 | 目标 |
| --- | ---: |
| `cross_family_scope_mix_count` | 0 |
| `unclassified_job_projection_count` | 0 |
| `search_only_job_profile_count` | 0 |
| `detail_artifact_trace_rate` | 1.0 |
| `ambiguous_experience_projection_count` | 0 |
| `confirmed_experience_scope_trace_rate` | 1.0 |
| `duplicate_gate_record_count` | 0 |

## 2. 固定案例

1. 后端 + AI 应用三个 target role 拆成两个 scope。
2. 搜索结果中混入前端岗位，membership rejected 且不进入 family 分母。
3. 只有 search_page 的岗位保留 candidate，但不生成 JobInstanceRoleProfile。
4. 招聘 job_detail 或 confirmed official_job_detail 通过详情门禁。
5. experience_post 分别标为 role_family/company_role/job_instance/unknown，只有 confirmed link 投影。
6. 同公司同名多个 cluster 的 job_instance 面经产生 ambiguous。
7. 重放相同输入不新增 membership、detail receipt、scope link 或 snapshot。

## 3. 回归与真实边界

- 运行 WP3 聚焦单元/集成测试、WP2 CareerIntent 回归和全量 pytest。
- 执行 compileall、`git diff --check`。
- 本补丁通过只表示门禁机制 implemented/offline passed；WP3 authenticated-live 仍需真实
  recruitment detail、official detail 和 experience detail Artifact 后才能通过。
