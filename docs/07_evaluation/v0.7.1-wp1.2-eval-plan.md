# v0.7.1 WP1.2 Candidate 经历 Taxonomy Eval 计划

状态：Passed  
日期：2026-07-30

## E0 Schema

- Tool JSON Schema 包含 `claim_kind` discriminator；
- experience kind/context 和 capability level 出现显式 enum；
- 不再以 `value: Any` 作为所有新 Claim 的唯一模型协议。

## E1 Taxonomy

覆盖 employment、internship、research、project、competition、leadership、campus activity、
volunteering、entrepreneurship、teaching、training、other；覆盖课程、毕设、论文、学术科研、纵向、
横向、实习、工作、个人、开源等 context。

## E2 Open-world

- 中文/英文常见别名确定性归一化；
- 未知标签进入 `other/unknown` 并保留 raw label；
- 不因未知 kind/level 丢失整项经历或 capability。

## E3 Compatibility

- 旧 cache/fixture/predicate/value 可回放；
- 旧 snapshot 可读取；
- WP2 定向测试不回归。

## E4 Real DeepSeek

- 新 owner、真实 PDF、`cache_hit=false`；
- `effective_strategy=tool_calling`、thinking disabled；
- Pydantic 无 schema error；
- 领域拒绝项有 receipt，合法经历不因 kind/level 表达差异被丢弃；
- Profile 中 kind/context/raw label 与证据一致。

## 门禁

定向测试、WP2 回归、全量回归、`compileall`、`git diff --check` 和 E4 全部通过后标记 Passed。
