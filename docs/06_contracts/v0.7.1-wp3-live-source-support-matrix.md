# v0.7.1 WP3 Live Source Support Matrix

状态：Reviewed / WP3.1 Live Implementation Pending
日期：2026-08-06

`implemented` 仅表示存在 adapter 代码；`offline_passed` 只表示 fixture 可重放；
`authenticated_live` 必须有真实 Raw Artifact 和安全回执才能通过。

WP3.1 policy：招聘平台具体 job detail 是 Demand 默认主证据；官方详情只做条件触发的 escalation。
社区 search 命中不算可用证据，必须进入 post detail 并完成 typed segment 分类。

| Source | Channel | Search | Detail | 当前证据 | 结论 |
| --- | --- | --- | --- | --- | --- |
| `fixture_jobs` | recruitment | offline_passed | `job_detail` offline_passed | Role Graph 集成测试 | 仅回放；可验证 Demand 门禁 |
| `fixture_experience` | community | offline_passed | `experience_post` offline_passed | scope-link 集成测试 | 仅回放；尚未验证 typed segment 分流 |
| `official_careers` fixture | official escalation | offline_passed | `official_job_detail` offline_passed | detail gate 集成测试 | 仅验证可选升级 |
| `zhaopin_jobs` | recruitment | adapter implemented | 未实现 detail fetch | 当前 HTTP collect 固定归类 `search_page` | 阻塞默认 Demand live 闭环 |
| `nowcoder_experience` | community | adapter implemented，需 credential ref | 未实现 post detail fetch | 当前 HTTP collect 固定归类 `experience_search` | 阻塞 Community live 闭环 |
| generic `official_careers` | official escalation | entry fetch implemented | detail kind 未分类 | 当前成功页仍归类 `official_search` | 可选升级未完成，不阻塞默认路径 |
| `official_careers_meituan` | official escalation | 专用 detail API transport implemented | Raw kind 未对齐 | 当前仍归类 `official_search` | 可选升级未完成，不阻塞默认路径 |

## 下一步 WP3 必须完成

1. 招聘源实现受 allowlist 限制的 search result URL → job detail fetch，并只对真实详情页标记 `job_detail`。
2. 社区源实现 search result → post detail fetch，并将详情拆为 interview/employment typed segments。
3. 只有出现 escalation trigger 时才调用官方源；adapter 必须自行证明 `official_job_detail`，通用层不猜测。
4. 为每个 live source 分别记录 auth/CAPTCHA/rate-limit/source-changed 结果，不以 fixture 通过替代 live 验收。
