# v0.7.1 WP3 Live Source Support Matrix

状态：WP3.2 Implemented / Offline Passed / L1 Live Accepted / Community L2 Partial
日期：2026-08-12

`implemented` 仅表示存在 adapter 代码；`offline_passed` 只表示 fixture 可重放；
`authenticated_live` 必须有真实 Raw Artifact 和安全回执才能通过。

WP3.1 policy：招聘平台具体 job detail 是 Demand 默认主证据；官方详情只做条件触发的 escalation。
社区 search 命中不算可用证据，必须进入 post detail 并完成 typed segment 分类。

| Source | Channel | Search | Detail | 当前证据 | 结论 |
| --- | --- | --- | --- | --- | --- |
| `fixture_jobs` | recruitment | offline_passed | `job_detail` offline_passed | WP3.1 Role Graph 集成测试 | 仅回放；Demand 门禁通过 |
| `fixture_experience` | community | offline_passed | `experience_post` offline_passed | mixed exact-quote 集成测试 | 仅回放；typed segment 分流通过 |
| `official_careers` fixture | official escalation | offline_passed | `official_job_detail` offline_passed | detail gate 集成测试 | 仅验证可选升级 |
| `zhaopin_jobs` | recruitment | live passed | `job_detail` live passed | WP3.1 基线 3 个详情；WP3.1.2 当前 Scope 新增 1 个成都详情 Raw/Demand | **L1 accepted**；search-only projection=0，detail trace=1.0 |
| `nowcoder_experience` | community | Brave discovery implemented/offline passed | Crawl4AI 0.9.2 批量详情 implemented/offline passed | doctor 确认包、Chromium、credential ref；真实 Brave 和显式详情均 `authentication_required` | **L2 partial**；不访问 `/search`，snippet 投影=0 |
| `xiaohongshu_experience` | community | MediaCrawler REST contract passed offline | REST detail contract passed offline | 专用 MCP 已删除；doctor health=`adapter_required` | **L2 blocked**；本机 REST 服务未启动，0 live search/detail Raw |
| generic `official_careers` | official escalation | 条件计划/allowlist implemented | supported domain 才运行，否则 `adapter_required` | 非触发路径生成 `not_required` receipt | 可选升级；不阻塞默认 Demand |
| `official_careers_meituan` | official escalation | 专用 adapter 保留 | 条件升级路径 | 本轮未触发 live | 可选升级；不计入 L1/L2 |

## 剩余 Live 门禁

1. 刷新 Brave API Key 后重跑 `site:nowcoder.com` 受控 discovery。
2. 启动已固定版本、已确认许可证且正常登录的 MediaCrawler localhost REST 服务，完成 search/detail Raw。
3. 面经与工作体验各至少 3 个独立内容簇，并验证用途零串线和完整 quote 追溯。
4. 登录失效、风控、验证码或持续空结果继续记录为 blocked/partial，不降低门禁。
