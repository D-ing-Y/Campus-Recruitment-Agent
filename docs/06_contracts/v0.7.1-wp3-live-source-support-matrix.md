# v0.7.1 WP3 Live Source Support Matrix

状态：Reviewed / Live Implementation Pending
日期：2026-08-03

`implemented` 仅表示存在 adapter 代码；`offline_passed` 只表示 fixture 可重放；
`authenticated_live` 必须有真实 Raw Artifact 和安全回执才能通过。

| Source | Channel | Search | Detail | 当前证据 | 结论 |
| --- | --- | --- | --- | --- | --- |
| `fixture_jobs` | recruitment | offline_passed | `job_detail` offline_passed | Role Graph 集成测试 | 仅回放 |
| `fixture_experience` | experience | offline_passed | `experience_post` offline_passed | scope-link 集成测试 | 仅回放 |
| `official_careers` fixture | official | offline_passed | `official_job_detail` offline_passed | detail gate 集成测试 | 仅回放 |
| `zhaopin_jobs` | recruitment | adapter implemented | 未实现 detail fetch | 当前 HTTP collect 固定归类 `search_page` | live 未验收 |
| `nowcoder_experience` | experience | adapter implemented，需 credential ref | 未实现 post detail fetch | 当前 HTTP collect 固定归类 `experience_search` | live 未验收 |
| generic `official_careers` | official | entry fetch implemented | detail kind 未分类 | 当前成功页仍归类 `official_search` | 不能通过 detail gate |
| `official_careers_meituan` | official | 专用 detail API transport implemented | Raw kind 未对齐 | 当前仍经通用 `_default_kind` 归类 `official_search` | 不能通过 detail gate |

## 下一步 WP3 必须完成

1. 招聘源实现受 allowlist 限制的 search result URL → job detail fetch，并只对真实详情页标记 `job_detail`。
2. 经验源实现 search result → post detail fetch，只对帖子详情标记 `experience_post`。
3. 官方源依 adapter 证明区分 `official_search` 与 `official_job_detail`，不允许通用请求层自行猜测。
4. 为每个 live source 分别记录 auth/CAPTCHA/rate-limit/source-changed 结果，不以 fixture 通过替代 live 验收。
