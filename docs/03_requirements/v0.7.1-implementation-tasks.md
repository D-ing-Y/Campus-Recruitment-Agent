# v0.7.1 实现任务清单

状态：Ready for Implementation（WP0 Passed；WP1 Not Started）  
日期：2026-07-28  
关联需求：`docs/03_requirements/v0.7.1-vertical-workflow-closure-and-cli.md`  
关联 RFC：`docs/04_rfc/0008-cli-observability-and-vertical-workflow-closure.md`  
关联 ADR：`docs/05_adr/0008-adopt-cli-first-observable-workflow-hardening.md`

本清单只记录实际完成状态。未运行对应测试、真实 smoke 或人工复核时不得勾选。

## 1. 文档门禁

- [x] 新增 v0.7.1 Requirements、RFC、ADR、CLI/Run contract 和 Eval plan。
- [x] 建立 `docs/09_versions/v0.7.1/` 版本入口和验收台账。
- [x] 明确 v0.7.1 不实现 v1.0 Parent Graph。
- [x] 明确 fixture、DeepSeek smoke、live source smoke 和真实案例分层。
- [ ] WP1 开发前增量更新 Evidence/LLM/Candidate contracts 的 predicate、receipt 和 batch 语义。
- [ ] WP2 开发前增量更新 CareerIntent/TargetDecision/State/Human contracts。
- [ ] WP3 开发前复核 Source/Role contracts 与 live support matrix。
- [ ] WP4-WP6 开发前复核 Matching/Preparation/Feedback/Handoff contract delta。
- [ ] 实现发现跨模块语义缺口时先更新文档。

## 2. 基线冻结

- [x] 记录 Git commit、Python/package 版本和当前工作区状态。
- [x] 运行并记录 v0.1-v0.7 全量测试基线。
- [x] 运行旧 CLI 黑盒基线，记录只调用 Mini Runtime 的事实。
- [ ] 保存真实 Candidate 失败的脱敏统计与本地 artifact refs。
- [ ] 为 Candidate predicate 错位建立最小可提交失败 fixture。
- [ ] 盘点每个 Graph 的名义节点、真实责任、输出和失败边界。
- [ ] 标出 Matching/Preparation/Feedback no-op 或聚合式节点。

## 3. WP0 RuntimeFactory 与路径

- [x] 实现 cwd-independent 项目配置/data root 解析。
- [x] 实现统一 RuntimeFactory。
- [x] 装配 Evidence/Profile/Role/Matching/Preparation/Feedback repositories。
- [x] 装配 BlobStore、各 Graph SQLite checkpointer、LLM provider/cache。
- [x] 装配 ToolRegistry、SourceAdapter registry 和 CredentialResolver。
- [x] 实现 `campus-agent doctor` 并脱敏展示依赖状态。
- [x] 测试从仓库外临时目录调用已安装 `campus-agent`。

## 4. WP0 RunSession 与可观测性

- [x] 实现 RunSession repository、version/CAS 和 current refs 校验。
- [x] 实现 RunManifest/Event/Error/LLM/ArtifactIndex/Handoff schema。
- [x] 实现 append-only event sink 和安全 artifact writer。
- [x] 失败时仍写 terminal manifest/error event。
- [x] 修复 Candidate 固定 success/零 duration trace。
- [x] 修复节点计数读取旧 state 的问题。
- [x] 提供各 Graph 可复用的 node/tool/LLM/route/interrupt 事件适配基座；逐图接线留在 WP1-WP6。
- [x] 实现 redaction policy 和 secret/private-content tests。
- [x] 明确 checkpoint、Run artifact、Evidence Store 和 Session 边界。

## 5. WP0 CLI skeleton

- [x] 默认无子命令进入交互式引导。
- [x] 实现 one-shot command group 和全局 `--json`。
- [x] 实现 `session start/status/resume/history`。
- [x] 实现 `inspect run/node/llm/evidence/claims/profile/handoff`。
- [x] JSON stdout 不包含 banner/prompt/log。
- [x] 实现稳定退出码与业务状态分离。
- [x] `run` 旧 Mini Runtime 明确标记 legacy 或迁移为新入口。
- [x] CLI subprocess 测试覆盖任意 cwd、human、JSON 和错误码。

## 6. WP1 Candidate predicate 与持久化

- [ ] 定义 versioned Candidate predicate contract。
- [ ] 同步 prompt、ExtractedClaim schema、领域 Validator 和 Projector。
- [ ] 为每个模型输出项生成 accepted/rejected ValidationReceipt。
- [ ] reject reason 保留 item index、fragment refs 和 schema/prompt version。
- [ ] 定义原子 Claim batch 或明确逐项事务语义。
- [ ] 定义 processed fragment 的 accepted/all-rejected/retryable/fatal 状态。
- [ ] 实现稳定 education/experience record ID 与多记录投影。
- [ ] 不可投影 predicate 不得进入 active Candidate Claim set。
- [ ] schema/prompt 升级后可从 immutable raw 证据显式 rebuild。

## 7. WP1 Candidate Graph 与 CLI

- [ ] 真实 PDF/README → Artifact/Fragment/locator。
- [ ] DeepSeek claim candidate → domain validation → Claim。
- [ ] Claim → CandidateProfile snapshot，accepted 支持项 100% 可投影/追溯。
- [ ] Sufficiency/unknown 与真实画像内容一致。
- [ ] ask/upload/correct/skip/cancel CLI interaction。
- [ ] interrupt 跨进程 resume。
- [ ] duplicate input/resume 零重复 Artifact/Claim/Snapshot。
- [ ] model/storage/checkpoint/fatal validation failure 可诊断。
- [ ] 用户人工复核真实候选人画像。

## 8. WP2 CareerIntent intake

- [ ] 原始求职意图先归档为用户 evidence。
- [ ] 实现 structured intent candidate 和 validator。
- [ ] 确定 CareerIntent canonical source，消除扁平字段/constraints 双写漂移。
- [ ] CLI 展示并确认 hard/negotiable/preference。
- [ ] 成都保存为 confirmed hard location constraint。
- [ ] 2027 和校招范围由用户确认，不擅自补充秋招/春招。
- [ ] 大型企业/互联网科技公司保存为 negotiable preference。
- [ ] 发布 versioned CareerIntent snapshot。
- [ ] 确定性投影 SearchScope 并记录版本。
- [ ] 普通偏好修改只 rematch；scope 变化产生 role research handoff。

## 9. WP3 Role live source

- [ ] 定义并输出 live adapter support matrix。
- [ ] CareerIntent snapshot → SearchScope → query plan。
- [ ] recruitment discovery search raw-first。
- [ ] 至少一个被接受岗位具有真实 detail raw Artifact。
- [ ] search page/card 不冒充 job detail。
- [ ] official entry/domain/allowlist 来源可追溯。
- [ ] official raw → JobIdentityLink → FieldResolution。
- [ ] experience search/detail 分离验收。
- [ ] auth interrupt/resume 与 credential ref redaction。
- [ ] CAPTCHA/rate limit/risk/source changed 返回结构化阻塞。
- [ ] JobInstanceRoleProfile/RoleFamilyProfile 事实全部可追溯。
- [ ] live 失败与 replay fixture 结果分开记录。

## 10. WP4 Matching 与 Decision

- [ ] 只消费 WP1-WP3 真实 current snapshots。
- [ ] qualification/alignment/coverage/preference 节点拆实或诚实合并。
- [ ] 每个真实节点产生 output refs、计数、duration 和 reason codes。
- [ ] 四类 Gap 分离，unknown 不等于 failed。
- [ ] 确定性评分/排序可复算。
- [ ] LLM explanation 不修改事实/数字并通过 validator。
- [ ] CLI select/defer/reject/revise/refresh/cancel。
- [ ] current TargetDecision 批次原子持久化、重复响应幂等。
- [ ] candidate/intent/role 变化形成正确 typed handoff。

## 11. WP5 Constraints 与 Preparation

- [ ] CLI 首次创建/确认/version PreparationConstraints。
- [ ] selected target 缺失时返回 `target_selection_required`。
- [ ] objective/activity/priority/package/schedule 节点拆实或诚实合并。
- [ ] 每个真实节点持久化/返回可检查输出 refs。
- [ ] P0-P4、MinimumPreparationPackage、DAG、capacity、deadline 可重放。
- [ ] unaddressable blocker 生成 target review，不生成虚假学习任务。
- [ ] capacity shortage 保留 partial/deferred。
- [ ] CLI accept/revise/exclude/defer/cancel。
- [ ] constraints revision 产生新计划版本并 supersede 旧版本。

## 12. WP6 Feedback

- [ ] feedback ingestion 的 Graph 内外责任与事件语义一致。
- [ ] observation/diagnosis/attribution 节点拆实或诚实合并。
- [ ] 明确评价路径 raw-first、归因确认、Claim/directive 正确。
- [ ] 无解释拒绝只保存 outcome observation。
- [ ] task completion 只更新 progress。
- [ ] 单次反馈不修改 RoleFamily common/frequent。
- [ ] duplicate event/response 零重复业务写入。
- [ ] checkpoint/repository/raw-write/LLM failure 可恢复或安全终止。

## 13. WP6 HandoffDispatcher

- [ ] 实现 `handoff list/consume/resolve`。
- [ ] candidate profile rebuild handler。
- [ ] intent review handler。
- [ ] role instance refresh handler。
- [ ] role family aggregation candidate handler。
- [ ] rematch handler。
- [ ] replan handler。
- [ ] 每个 handler 校验 owner、origin refs 和 successor refs。
- [ ] 同一 handoff 最多一次成功 resolution。
- [ ] dispatcher 每次只消费一个 handoff，不自动递归循环。

## 14. WP7 串联验收

- [ ] 同一 Session 完成 Candidate + Intent。
- [ ] 从 confirmed Intent 发起 Role real research。
- [ ] 用真实 snapshots 运行 Matching 并保存 TargetDecision。
- [ ] 创建 Constraints 并生成/确认 LearningPlan。
- [ ] 添加明确反馈并完成 attribution。
- [ ] 消费 directive，生成 successor snapshot。
- [ ] rematch 并 replan，保留完整 version chain。
- [ ] 在两个不同 interrupt 点关闭进程并恢复。
- [ ] 重放一次输入和一次 handoff，确认零重复写。
- [ ] 所有步骤可由 `inspect` 查询，不直接解析 checkpoint msgpack。

## 15. Eval、失败与安全

- [x] WP0 Unit/contract/repository/node 与 Candidate regression 测试通过。
- [x] 已安装 CLI subprocess 测试通过。
- [ ] Failure injection 覆盖 LLM/tool/storage/checkpoint/source/duplicate/stale/budget。
- [ ] DeepSeek Candidate semantic smoke 通过。
- [ ] Role opt-in live search→detail→official status smoke 通过或诚实 Partial。
- [ ] Eval 指标从实际 Graph/CLI 产物计算。
- [x] WP0 未写入真实材料/live raw/API key/Cookie；新增 artifact 隐私测试通过。
- [x] 全量 v0.1-v0.7 回归通过。

## 16. 版本收口

- [ ] 新增 `docs/07_evaluation/v0.7.1-eval-report.md`，计划与实际结果分开。
- [x] 更新 execution log 和 acceptance matrix 的 WP0 实际证据。
- [ ] 更新 Requirements/RFC/ADR/Tasks 状态为 Implemented / Accepted。
- [ ] 更新 README、Roadmap、Architecture、contracts 和版本号。
- [x] 记录新增测试数、全量测试数、WP0 smoke 时间/状态和已知限制。
- [x] 基于真实变更和测试结果生成中文提交信息（仅建议，未提交）。

## 17. 明确禁止

- 不把旧 Mini Runtime success 当完整业务 success。
- 不把 fixture、预填 predicted、布尔 gold 或 schema parse 当真实语义质量。
- 不保留伪 success/no-op 节点来满足拓扑数量。
- 不把搜索页/搜索卡当岗位详情页。
- 不绕过登录、验证码、限流或风险控制。
- 不把 Session、Run artifact 或 checkpoint 当事实源。
- 不让 dispatcher 变成隐藏 Parent Graph。
- 不提交真实个人材料、live raw、Cookie 或 API key。
