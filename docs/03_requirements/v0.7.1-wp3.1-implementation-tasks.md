# v0.7.1 WP3.1 实施任务：岗位需求与评价画像分流

状态：WP3.1.2 Implemented / Offline Passed（L1 accepted，multi-platform L2 partial）
日期：2026-08-12

说明：本文件记录实际完成状态；live 未通过项即使代码已实现也保持未勾选或明确 partial/blocked。

## 1. 文档与边界

- [x] Requirements：定义平台详情默认主路径、可选官网升级和画像分流。
- [x] RFC：定义 RecruitmentCollection、CommunityCollection 和两个投影阶段。
- [x] ADR：记录采纳方案、兼容策略和被拒绝替代方案。
- [x] Contract：定义 group、query plan、typed segment、profiles 和 bundle。
- [x] Eval Plan：定义离线、集成和受控 live 验收。

## 2. RecruitmentCollection

- [x] 为启用的招聘平台实现 `search -> platform detail -> Raw Artifact`。
- [x] 禁止 search card/snippet 直接创建 JobDemandProfile。
- [x] 将平台 detail document kind 纳入 RoleDetailEvidenceReceipt allowlist。
- [x] 保留 retrieved_at、平台岗位 ID、URL、字段缺失、冲突和关闭状态。
- [x] 招聘详情规范化后执行 JobIdentity、family membership 和去重。
- [x] 按 `company_key + role_family_id` 建立幂等 CompanyRoleGroup。
- [x] 把 employer official 从默认节点改为条件触发的 OfficialVerificationPlan。
- [x] 复用通用 allowlist/ATS 路由；未支持公司返回 `adapter_required`，不动态生成爬虫代码。

## 3. CommunityCollection

- [x] 从 CompanyRoleGroup 生成有界 CommunitySearchPlan。
- [x] 分别限制 group/source/query/detail 预算并记录 expansion reason。
- [x] 为启用的社区来源实现 `search -> post detail -> Raw Artifact`。
- [x] 建立 CommunityEvidenceDocument 分类器与严格输出 Schema。
- [x] 建立片段级 CommunityEvidenceSegment extractor。
- [x] mixed 文档按独立 quote Fragment 拆分 interview 与 employment segment。
- [x] 校验 company、role family、job scope；ambiguous 内容不得投影。
- [x] 校验 segment type -> usage allowlist，拒绝跨用途输入。
- [x] 对 detail、segment 和 role response 实现稳定 ID/幂等写入。

## 4. Profile Projection

- [x] 实现 JobDemandProfile projector。
- [x] 实现 RoleFamilyDemandProfile projector，并分开 JD 与面经分母。
- [x] 实现 JobReputationProfile projector。
- [x] 实现 CompanyReputationProfile projector，保留 role distribution。
- [x] 样本不足、单一来源和冲突分别标记，不生成 overall score。
- [x] 创建 RoleIntelligenceBundle，只保存 typed references。
- [x] 保留旧 RoleProfileSnapshot/hiring_signals 只读，不猜测迁移。

## 5. Graph、持久化与 CLI

- [x] 在现有 RoleProfileGraph 内增加 group、community classification、Demand 和 Reputation 阶段。
- [x] 分离 recruitment 与 community 的失败、预算和恢复状态。
- [x] 增加新 Contract 的 additive persistence，不覆盖旧 Snapshot。
- [x] CLI 展示 Demand、Job Reputation、Company Reputation 和 missing sections。
- [x] Role checkpoint 只保存有界 ToolResult 摘要，不持久化详情页正文或未脱敏异常。
- [ ] 具体问答展示 JD/面经/主观评价标签及引用。
- [x] 保持现有 RoleProfileGraph/CLI 外部入口，避免无必要的拓扑复制。

## 6. 下游消费者

- [x] Matching/GapAssessment 只接受 Demand requirements。
- [x] Preparation 只接受 Demand requirements 与 assessment signals。
- [x] TargetDecision 可读取 Reputation，但不得改写 Demand 或能力 Gap。
- [x] 在 runtime contract 层实现 consumer allowlist，不只依赖 prompt。

## 7. 测试

- [x] 单元：document/segment 分类、usage allowlist、scope、样本语义、无 overall score。
- [x] 单元：search-only、unknown、ambiguous 和无 quote 内容投影数为 0。
- [x] 集成：平台 detail 可在无官网证据时发布 Demand。
- [x] 集成：平台冲突/疑似过期/缺字段/用户要求才触发 official escalation。
- [x] 集成：mixed post 同时产生两个独立、可追溯且不交叉消费的 segment。
- [x] 集成：company-only 评价不进入 JobReputationProfile。
- [x] 集成：同一 company 多岗位聚合保留 role distribution 和冲突。
- [x] 回归：详情提取正文不进入 checkpoint，`tool_results` 稳定保持有界大小。
- [x] 回归：Role、Matching、Preparation、Feedback、Parent/CLI 聚焦测试。
- [x] 全量：pytest、compileall、`git diff --check`。

## 8. 受控 Live 验收

- [x] 至少一个招聘平台完成真实 search -> job detail -> Raw -> Demand。
- [ ] 至少一个社区来源完成真实 search -> post detail -> typed segment。
- [ ] 真实面经只进入 assessment signals。
- [ ] 真实在职体验只进入 Job/Company Reputation。
- [x] live 失败、登录限制、风控、空结果或解析不足如实记录，不以 fixture 冒充。
- [ ] WP3.1 Eval 全部通过后更新版本矩阵、执行日志和下一工作包状态。

## 9. WP3.1.1 社区检索分层与多平台补丁

- [x] Requirements/RFC/ADR/Contracts：固化三轮、双来源、详情 scope 与 Sidecar 边界。
- [x] 扩展 CommunitySearchQuery，并新增 AttemptReceipt 与 Coverage。
- [x] 实现岗位族展示词典和三层稳定查询生成器。
- [x] 将 Graph 改为 plan -> collect -> detail -> classify -> assess -> route 的确定性循环。
- [x] 实现每用途两篇停止、主备来源切换、三轮与全局预算硬上限。
- [x] 实现 canonical URL、平台 ID、正文 hash 跨轮去重。
- [x] 扩展 SourceCapabilities.authorization_mode 和 external locator。
- [x] 实现只读 Social Media MCP Bridge 与 Xiaohongshu adapter；不 vendoring MediaCrawler。
- [x] 完成假 Sidecar contract、Graph 集成、全量回归与安装态 CLI 冒烟。
- [x] 分别执行牛客与小红书 live smoke；牛客为 partial，小红书内容链路为 blocked。

## 10. WP3.1.2 真实 L2 兼容补丁

- [x] Requirements/RFC/ADR/Contracts/Tasks/Eval：固化真实 Sidecar wire shape、别名和诊断边界。
- [x] 将 Bridge 对齐固定 MediaCrawler `/api`、8080 端口和真实文件下载协议。
- [x] 增加本轮文件隔离、关键词/post ID 过滤和真实上游响应 fixture。
- [x] 增加版本化公司品牌别名，保持 discovery 与详情 scope 分离。
- [x] 增加牛客 search diagnostic，区分非帖子卡、空结果、解析漂移、登录和风控。
- [x] 以 provider contract test 验证 DeepSeek V4 non-thinking Tool Calling；真实社区详情为 0，未伪造 live 模型调用。
- [x] 在仓库外安装并固定 MediaCrawler commit，确认许可证并完成 localhost Sidecar/MCP 启动。
- [ ] 完成小红书可见浏览器正常登录和内容抓取；当前页面导航超时且安全策略禁止跨浏览器绕行。
- [x] 运行牛客三级检索和小红书 MCP/search 尝试，并分别记录 partial/blocked。
- [x] 如实记录本轮 0 篇社区详情及牛客 partial、小红书 blocked 原因。
- [ ] 面经、工作体验各两篇独立详情通过内容级 L2 门禁。
- [x] 更新支持矩阵、执行日志、验收矩阵和 Roadmap。
