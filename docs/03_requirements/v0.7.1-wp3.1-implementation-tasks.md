# v0.7.1 WP3.1 实施任务：岗位需求与评价画像分流

状态：Ready for Implementation
日期：2026-08-06

说明：本文件只定义后续代码工作。本次文档补丁完成不代表下列实现项已完成。

## 1. 文档与边界

- [x] Requirements：定义平台详情默认主路径、可选官网升级和画像分流。
- [x] RFC：定义 RecruitmentCollection、CommunityCollection 和两个投影阶段。
- [x] ADR：记录采纳方案、兼容策略和被拒绝替代方案。
- [x] Contract：定义 group、query plan、typed segment、profiles 和 bundle。
- [x] Eval Plan：定义离线、集成和受控 live 验收。

## 2. RecruitmentCollection

- [ ] 为启用的招聘平台实现 `search -> platform detail -> Raw Artifact`。
- [ ] 禁止 search card/snippet 直接创建 JobDemandProfile。
- [ ] 将平台 detail document kind 纳入 RoleDetailEvidenceReceipt allowlist。
- [ ] 保留 retrieved_at、平台岗位 ID、URL、字段缺失、冲突和关闭状态。
- [ ] 招聘详情规范化后执行 JobIdentity、family membership 和去重。
- [ ] 按 `company_key + role_family_id` 建立幂等 CompanyRoleGroup。
- [ ] 把 employer official 从默认节点改为条件触发的 OfficialVerificationPlan。
- [ ] 实现通用 allowlist/ATS 路由；未支持公司返回 `adapter_required`，不动态生成爬虫代码。

## 3. CommunityCollection

- [ ] 从 CompanyRoleGroup 生成有界 CommunitySearchPlan。
- [ ] 分别限制 group/source/query/detail 预算并记录 expansion reason。
- [ ] 为启用的社区来源实现 `search -> post detail -> Raw Artifact`。
- [ ] 建立 CommunityEvidenceDocument 分类器与严格输出 Schema。
- [ ] 建立片段级 CommunityEvidenceSegment extractor。
- [ ] mixed 文档按独立 quote Fragment 拆分 interview 与 employment segment。
- [ ] 校验 company、role family、job scope；ambiguous 内容不得投影。
- [ ] 校验 segment type -> usage allowlist，拒绝跨用途输入。
- [ ] 对 detail、segment 和 resume response 实现稳定 ID/幂等写入。

## 4. Profile Projection

- [ ] 实现 JobDemandProfile projector。
- [ ] 实现 RoleFamilyDemandProfile projector，并分开 JD 与面经分母。
- [ ] 实现 JobReputationProfile projector。
- [ ] 实现 CompanyReputationProfile projector，保留 role distribution。
- [ ] 样本不足、单一来源和冲突分别标记，不生成 overall score。
- [ ] 创建 RoleIntelligenceBundle，只保存 typed references。
- [ ] 保留旧 RoleProfileSnapshot/hiring_signals 只读，不猜测迁移。

## 5. Graph、持久化与 CLI

- [ ] 在现有 RoleProfileGraph 内增加 group、community classification、Demand 和 Reputation 阶段。
- [ ] 分离 recruitment 与 community 的失败、预算和恢复状态。
- [ ] 增加新 Contract 的 additive persistence，不覆盖旧 Snapshot。
- [ ] CLI 展示 Demand、Job Reputation、Company Reputation 和 missing sections。
- [ ] 具体问答展示 JD/面经/主观评价标签及引用。
- [ ] 保持现有 RoleProfileGraph/CLI 外部入口，避免无必要的拓扑复制。

## 6. 下游消费者

- [ ] Matching/GapAssessment 只接受 Demand requirements。
- [ ] Preparation 只接受 Demand requirements 与 assessment signals。
- [ ] TargetDecision 可读取 Reputation，但不得改写 Demand 或能力 Gap。
- [ ] 在 runtime contract 层实现 consumer allowlist，不只依赖 prompt。

## 7. 测试

- [ ] 单元：document/segment 分类、usage allowlist、scope、样本语义、无 overall score。
- [ ] 单元：search-only、unknown、ambiguous 和无 quote 内容投影数为 0。
- [ ] 集成：平台 detail 可在无官网证据时发布 Demand。
- [ ] 集成：平台冲突/疑似过期/缺字段/用户要求才触发 official escalation。
- [ ] 集成：mixed post 同时产生两个独立、可追溯且不交叉消费的 segment。
- [ ] 集成：company-only 评价不进入 JobReputationProfile。
- [ ] 集成：同一 company 多岗位聚合保留 role distribution 和冲突。
- [ ] 回归：Role、Matching、Preparation、Feedback、Parent/CLI 聚焦测试。
- [ ] 全量：pytest、compileall、`git diff --check`。

## 8. 受控 Live 验收

- [ ] 至少一个招聘平台完成真实 search -> job detail -> Raw -> Demand。
- [ ] 至少一个社区来源完成真实 search -> post detail -> typed segment。
- [ ] 真实面经只进入 assessment signals。
- [ ] 真实在职体验只进入 Job/Company Reputation。
- [ ] live 失败、登录限制、风控、空结果或解析不足如实记录，不以 fixture 冒充。
- [ ] WP3.1 Eval 全部通过后更新版本矩阵、执行日志和下一工作包状态。
