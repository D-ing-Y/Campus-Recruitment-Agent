# v0.7.1 WP1.1 LangChain 接入层重构任务

状态：Completed  
日期：2026-07-30

## 1. 文档

- [x] Requirements、RFC、ADR、Contract、Tasks、Eval plan 完成。
- [x] 明确保留 LangGraph/领域/持久化，禁止全量 Agent 化。
- [x] 明确旧 Provider/Tool 兼容与 MCP 默认拒绝策略。

## 2. 基线与失败测试

- [x] 记录工作区、依赖和测试基线。
- [x] 为 strategy matrix、旧 profile 迁移、ToolSpec、MCP fixture 写失败测试。
- [x] 为 DeepSeek V3/reasoner capability 路由写测试。

## 3. Model 与 Structured Output

- [x] 增加受控 LangChain/provider integration 依赖。
- [x] 实现 ModelCapabilities、StructuredOutputPolicy/Response。
- [x] 实现 LangChainChatProvider 与 model factory。
- [x] RuntimeFactory/ModelProfileService 切换到新 factory。
- [x] Structured output 按 native/tool/json/mock 选择并记录 strategy。
- [x] cache key 增加 strategy/capability fingerprint。
- [x] 保留 retry、normalizer、usage、错误分类和 secret redaction。

## 4. Tool

- [x] 实现 ToolSpec 和 ToolRegistry 元数据注册。
- [x] 实现项目 Tool → LangChain StructuredTool adapter。
- [x] 默认 internal-only；模型绑定只接受 allowlist。
- [x] 拒绝重复名、非法 schema、未确认高风险 Tool。

## 5. MCP

- [x] 实现 MCPServerConfig 与配置校验。
- [x] 实现 MultiServerMCPClient adapter/catalog。
- [x] 实现 server/tool allowlist、命名冲突和 safe diagnostics。
- [x] stdio fixture 完成加载与调用；未授权和不可达用例失败可诊断。

## 6. 回归与验收

- [x] Candidate fixture 的 Claim/receipt/snapshot 语义不回退。
- [x] DeepSeek health + structured output smoke 记录实际 strategy。
- [x] 真实简历 E4 与 offline replay 重跑。
- [x] 单元、集成、CLI、全量回归、compileall、diff-check 通过。
- [x] 回填 execution log、acceptance matrix 和实际 Eval report。
- [x] 基于真实文件与验证结果生成中文 commit message，不执行提交。
