# v0.5 实现任务清单

状态：Implemented / Accepted
日期：2026-07-22
关联需求：`docs/03_requirements/v0.5-role-profile-graph.md`
关联 RFC：`docs/04_rfc/0005-role-profile-graph.md`
关联 ADR：`docs/05_adr/0005-separate-source-channels-and-role-profile-levels.md`

## 0. 2026-07-22 来源迁移决策

- [x] 停止继续尝试 BOSS；历史报告保留为决策证据，不再计入当前验收分母。
- [x] 将核心第三方招聘来源和契约 ID 改为 `zhaopin_jobs`。
- [x] 删除 `BossJobsAdapter`、BOSS parser、BOSS CLI credential 入口及对应运行时测试。
- [x] 从旧 recruitment-crawler Skill 归档迁移智联 HTML/结构化数据解析能力。
- [x] 实现 `ZhaopinJobsAdapter`、raw-before-parse、状态分类和本地 credential ref。
- [x] 完成智联离线 fixture、默认关闭、凭据脱敏、限流/结构变化测试。
- [x] 完成一次智联小范围 opt-in live smoke和真实 raw 重放解析。
- [ ] 完成至少一条“智联候选 → 企业官网岗位”的真实身份链接与字段核验。

## 1. 文档门禁

- [x] 确认 v0.5 Requirements、ADR 和 RFC。
- [x] 确认 Source Collection 与 Role Profile contract。
- [x] 更新 State、Evidence、LLM、Tool 和 Human Interaction contract。
- [x] 确认 fixture、指标、live smoke 和验收边界。
- [x] 将“第三方发现 → 企业官网字段级核验 → 经验信号”写入 v0.5 核心文档。
- [x] 完成 `docs/07_evaluation/v0.5-source-feasibility-report.md` 的 P0 准入门禁。
- [x] 完成 BOSS、牛客和企业官网三个 P1 浏览器来源 smoke，并记录无凭据运行收据。
- [x] 实现中若需改变跨模块契约，先更新关联文档。

## 2. 依赖与目录

- [x] 新增 `sources/` 或等价模块，第三方发现、官网核验和经验 adapter 分目录。
- [x] 新增 `workflows/role_profile/`。
- [x] 新增 `schemas/source.py`、`schemas/role_graph.py`，升级 `schemas/role.py`。
- [x] 新增 role query、job normalization、experience extraction、role sufficiency prompt。
- [x] 新增 source run 的本地数据目录与 `.gitignore` 规则。
- [x] 新增 `third_party/reference_sources/` 本地参考目录和可追溯 manifest；
  第三方源码目录不提交 Git。
- [x] 保留 v0.1-v0.4 Graph、schema 和测试入口。

## 3. Schema

- [x] 实现 `SearchScope`、`RoleQueryPlan`、`SourceQuery`。
- [x] 实现 `SourceDocument`、`SourceBatch`、`SourceRunReceipt`。
- [x] 实现 `NormalizedJobPosting`、`ExperienceEvidenceRecord`。
- [x] 实现 `JobPostingCluster`、`OfficialVerificationPlan`、`JobIdentityLink`、
  `FieldResolution` 和冲突记录。
- [x] 将 RoleProfile 升级为 v0.5 的 job instance / role family 契约。
- [x] 实现 `RoleCoverageGap`、`RoleCoverageAssessment`。
- [x] 实现 `RoleProfileGraphState`、budget、counter 和 reducer。
- [x] 保持 v0.3 RoleProfile snapshot 可读取。

## 4. SourceAdapter 基础设施

- [x] 定义 recruitment discovery/official verification/experience adapter protocol。
- [x] 实现 fixture recruitment adapter。
- [x] 实现 fixture experience adapter。
- [x] 实现分页/cursor、限速、超时、有限重试和错误分类。
- [x] 实现 raw-before-parse 原子边界。
- [x] 实现 query/source batch 幂等键与 SourceRunReceipt。
- [x] 实现 credential ref 读取边界，确保秘密值不进入 State/trace/evidence。
- [x] 实现 `authentication_required`、`rate_limited`、`source_changed` 和 `robots_disallowed`。

## 5. 首版真实来源与准入门禁

- [x] 完成招聘发现平台范围决策：智联为核心，BOSS 因持续风控退出运行时，
  前程无忧/51job 进入 backlog，并记录候选项目的 adopt/reference/reject 结论。
- [x] 审查牛客、小红书等经验来源候选项目并记录 adopt/reference/reject 结论。
- [x] 审查通用 crawler、正文抽取、JSON-LD 和 ATS adapter 候选项目。
- [x] 每个候选记录 repository、commit、license、维护状态、凭据方式、入口、风险和 smoke。
- [x] 未声明许可证、违反来源边界或依赖规避风控的项目不得并入业务代码。
- [x] 浏览器验证 BOSS 登录态下的 `AI Agent` 搜索和岗位详情读取。
- [x] 浏览器验证牛客登录态下的 `京东 AI Agent 面经` 搜索和帖子详情读取。
- [x] 浏览器验证“BOSS 京东科技候选 → 京东校招官网”的二次核验，并得到
  `official_not_found`，未将第三方岗位自动判为虚假。
- [x] 实现 `zhaopin_jobs` live discovery adapter 并删除 `boss_jobs` 运行时代码。
- [x] 实现 `official_careers` live verification adapter。
- [x] 实现 `nowcoder_experience` live adapter。
- [x] 实现本地 Copy as cURL/Cookie 导入命令或 service。
- [x] 验证用户正常登录后的 live 授权恢复，不使用 Playwright 绕过验证（`v05-live-auth-resume-20260721-e`）。
- [x] live collector 默认关闭，只有显式 config 才启用。
- [x] 每个 adapter 生成不含凭据的 SourceRunReceipt。
- [x] 三个 adapter 分别执行一次本地 opt-in live smoke：智联、牛客与企业官网均通过。
- [ ] 至少完成一条“智联候选 → 企业官网岗位”的真实身份链接与字段核验。

## 6. 原始归档与文本处理

- [x] 每个搜索/详情响应先保存不可变 EvidenceArtifact。
- [x] HTML/JSON/text 抽取生成版本化 DocumentExtraction。
- [x] Fragment 保留 DOM selector、JSONPath、行号或字符范围。
- [x] source URL、published_at、retrieved_at、query ID 和 adapter version 进入 provenance。
- [x] 空页、登录页、验证码页和结构变化页不得当作成功岗位记录。
- [x] 解析升级可从 raw Artifact 重放。

## 7. 归一化、身份链接与冲突消解

- [x] 实现招聘岗位 strict JSON normalizer。
- [x] 实现经验帖 strict JSON extractor。
- [x] 未知字段显式为空/null/unknown。
- [x] 实现 hard scope 校验和机器可读排除原因。
- [x] 相关噪声记录保留 deferred，不静默删除。
- [x] 实现 exact normalized job key 聚类。
- [x] 实现 fuzzy merge candidate，不允许 LLM 直接合并。
- [x] 跨平台岗位和跨帖转载去重后仍保留全部 source refs。
- [x] 为第三方岗位 cluster 生成有界 `OfficialVerificationPlan`。
- [x] 实现官网域名/招聘入口解析、同域预算和 redirect allowlist。
- [x] 实现 JSON-LD JobPosting → 已注册 ATS/site adapter → DOM/text → LLM JSON 的解析链。
- [x] 实现 `JobIdentityLink`，标题相似只能形成 candidate。
- [x] 实现字段级 `FieldResolution`，不得整条官网记录覆盖第三方记录。
- [x] 官网未找到、不可访问和身份模糊分别结构化保存。
- [x] 冲突按 source authority、freshness 和 identity policy 保存和解析。
- [x] 未知站点只生成声明式 `OfficialSiteAdapterSpec` 候选，不在运行时生成并执行代码。

## 8. Role Claim 与画像投影

- [x] 实现 recruitment Role Claim extractor/validator。
- [x] 实现 experience signal Claim extractor/validator。
- [x] 校验每种 predicate 允许的 source channel/authority。
- [x] 实现 JobInstanceRoleProfile projector。
- [x] 实现 RoleFamilyProfile deterministic aggregator。
- [x] 计算 requirement prevalence、company coverage 和 signal frequency。
- [x] 样本不足时禁止输出 common/universal 表述。
- [x] 实现 snapshot canonical hash、版本复用和版本比较。

## 9. Role Profile Subgraph

- [x] 实现 `initialize_role_run`。
- [x] 实现 `plan_role_queries`。
- [x] 实现 `collect_and_archive_sources`。
- [x] 实现 `extract_and_normalize_sources`。
- [x] 实现 `deduplicate_source_records`。
- [x] 实现 `plan_official_verification`。
- [x] 实现 `collect_and_archive_official_sources`。
- [x] 实现 `link_official_job_records`。
- [x] 实现 `resolve_job_field_conflicts`。
- [x] 实现 `extract_and_validate_role_claims`。
- [x] 实现 `project_job_instance_profiles`。
- [x] 实现 `aggregate_role_family_profile`。
- [x] 实现 `assess_role_coverage`。
- [x] 实现确定性 `route_role_next_action`。
- [x] 实现 `interrupt_for_source_auth` 和 `validate_source_authorization`。
- [x] 实现 `finalize_role_profiles`。
- [x] 编译 conditional edges、loop、checkpoint 和停止边。

## 10. 动态查询与授权

- [x] deterministic query planner/evaluator 作为离线 baseline。
- [x] 版本化 LLM query planner/coverage evaluator。
- [x] 实现 search_more/change_query/change_source/verify_official 策略。
- [x] 防止重复 query、重复 source 和无预算换词。
- [x] 授权 interrupt 不携带 Cookie/cURL 正文。
- [x] authorized/skip_source/cancel resume 行为可恢复且幂等。
- [x] 进程重启后从 source collection/auth interrupt 恢复。

## 11. Trace、Report 与安全

- [x] trace 记录 query/source/record/count/route 摘要。
- [x] report 输出来源覆盖、样本定义、freshness、unknown/conflict 和 evidence refs。
- [x] 输出 search_scope、user_needs、query history、source index/receipts 和 normalized JSONL。
- [x] 不记录完整网页、Cookie、Authorization、cURL 或私有凭据路径内容。
- [x] 验证 source URL、redirect、路径和 owner 边界。
- [x] 对 robots/terms 禁止或无法安全采集的来源结构化停止。

## 12. 测试

- [x] Source/Role/State schema 与 reducer 单元测试。
- [x] 三类 fixture adapter contract 测试。
- [x] raw-before-parse 测试。
- [x] discovery/official/experience normalizer 测试。
- [x] hard scope 保留/排除测试。
- [x] exact/fuzzy/cross-source dedup 测试。
- [x] 官网解析链、域名预算和 AdapterSpec 校验测试。
- [x] JobIdentityLink 正反例与字段级 FieldResolution 测试。
- [x] official_not_found/unavailable/identity_ambiguous/adapter_required 测试。
- [x] runtime 不执行 LLM 生成代码的安全测试。
- [x] source authority violation 测试。
- [x] job instance 与 role family projector/aggregator 测试。
- [x] prevalence 分母、公司多样性和样本不足测试。
- [x] search_more/change_query/change_source/verify_official 路由测试。
- [x] auth interrupt/resume 和 credential redaction 测试。
- [x] SQLite checkpoint 重启与重复 batch 幂等测试。
- [x] rate limit/source changed/网络/解析/LLM/存储错误测试。
- [x] v0.1-v0.4 全量回归。

## 13. Eval 与收口

- [x] 计算 v0.5 业务指标。
- [x] 记录离线 fixture 结果与 live smoke 结果，二者分开展示。
- [x] 新增 `docs/07_evaluation/v0.5-eval-report.md`。
- [x] 将 Requirements/RFC 状态更新为 Implemented（3/3 live smoke 与真实 confirmed 官网链接均通过）。
- [x] 勾选本任务清单并记录真实测试结果。
- [x] 将 README、Roadmap 和架构更新为 v0.5 Implemented / Accepted。
- [x] 不因 live source 临时不可用伪造 smoke 成功；保留 Partial 状态和失败证据。

## 14. 明确禁止

- 不在解析或模型总结前跳过原始响应归档。
- 不让社区面经确认岗位存在、硬性资格或截止时间。
- 不让 LLM 直接合并重复岗位、计算分母或修改统计阈值。
- 不让 LLM 在 live run 中生成并立即执行 Python/JavaScript 爬虫。
- 不把“官网未找到”自动解释成第三方岗位虚假。
- 不在字段级 authority/freshness/identity resolution 前整条覆盖来源记录。
- 不根据 CandidateProfile 能力静默过滤岗位。
- 不把 Cookie/cURL/Authorization 写入 State、trace、Artifact metadata、报告或 Git。
- 不绕过验证码、登录、限流、robots 或来源条款。
- 不在 v0.5 引入匹配评分、RAG、分布式存储或 Multi-Agent。

## 15. 2026-07-22 实现与验收结果

- v0.5 新增测试：72 项，全部通过。
- 全量测试：140 项，全部通过；v0.1-v0.4 的 68 项回归全部保留。
- 默认测试完全离线；live adapter 默认关闭。
- DeepSeek `deepseek-v4-flash` 真实结构化调用、牛客 raw-first + 17 条重放归一化、企业官网 raw-first 和真实 auth resume 通过。
- BOSS 风险码 `37` 已归档且未重试/绕过；该来源已退出运行时，不再作为未完成项。
- `v05-live-confirmed-20260722-c` 完成真实智联候选到美团官网同岗确认：4 个强身份信号、
  `confirmed` JobIdentityLink、40 条 FieldResolution，其中 13 条由官网主权威完成冲突裁决。
- Eval 报告：`docs/07_evaluation/v0.5-eval-report.md`。
- v0.5 验收项已全部完成。
