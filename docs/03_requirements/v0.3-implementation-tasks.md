# v0.3 实现任务清单

状态：Completed（45/45 tests passed）
日期：2026-07-15
关联需求：`docs/03_requirements/v0.3-evidence-domain-foundation.md`
关联 RFC：`docs/04_rfc/0003-evidence-domain-foundation.md`

## 1. 文档与契约

- [x] 确认 evidence、candidate、intent、role、gap schema。
- [x] 确认 SQLite 表、迁移和文件目录。
- [x] 确认 Claim 类型、引用校验和幂等键。
- [x] 准备匿名简历、项目说明、JD 和错误 Claim fixture。

## 2. Schema

- [x] 新增 `schemas/evidence.py`。
- [x] 新增 `schemas/candidate.py`。
- [x] 新增 `schemas/intent.py`。
- [x] 新增 `schemas/role.py`。
- [x] 新增 `schemas/gap.py`。
- [x] 更新 schema 导出和版本字段。

## 3. Storage 与 Repository

- [x] 定义 `BlobStore`、`EvidenceRepository`、`ProfileRepository` protocol。
- [x] 实现本地 blob store 与原子写入。
- [x] 实现 SQLite 连接和 schema migration。
- [x] 实现 artifact/fragment/claim/profile repository。
- [x] 实现 hash 去重、幂等写入和失败清理。

## 4. Evidence Pipeline

- [x] 实现 Artifact ingestion。
- [x] 实现 TXT/Markdown/HTML fixture 文本读取；PDF 只完成登记或简单提取。
- [x] 实现确定性 Fragmenter 和 locator。
- [x] 实现 ClaimValidator。
- [x] 实现 ProfileSnapshot 的最小保存与读取。

## 5. LLM 复用升级

- [x] 从 `parse_search_goal_with_llm` 提炼泛型 structured output helper。
- [x] 保持 v0.2 SearchGoal 路径兼容。
- [x] 新增版本化 Claim extractor prompt/schema。
- [x] 复用 mock provider 测试合法、非法、重试和 provider error。
- [x] 确保 cache key 包含 prompt/schema/fragment hash。

## 6. Capability Ontology

- [x] 定义首版 ontology 文件和版本。
- [x] 实现 canonical name/alias 查找。
- [x] 未匹配项保留 raw label，不静默映射。

## 7. Trace、Report 与 Eval

- [x] trace 增加 artifact/fragment/claim 摘要，不记录完整敏感文本。
- [x] 输出 evidence pipeline report。
- [x] 实现 evidence trace、unsupported claim、locator、dedup 指标。
- [x] 生成 v0.3 eval report。

## 8. 测试与验收

- [x] schema 单元测试。
- [x] blob/repository/migration 单元测试。
- [x] dedup、幂等和失败清理测试。
- [x] Claim 引用存在性与类型约束测试。
- [x] Artifact→Fragment→Claim 集成测试。
- [x] Candidate/Intent/Role snapshot round-trip 测试。
- [x] v0.1/v0.2 全量回归测试。
- [x] 更新 README、roadmap、contracts 和验收报告。

## 9. 明确禁止

- 不提交真实简历、Cookie、API key 或个人敏感数据。
- 不在 Agent 节点直接写 SQL 或本地路径。
- 不让 LLM 绕过 ClaimValidator 直接写入确认画像。
- 不在 v0.3 同时实现 RAG、分布式存储、真实爬虫或 Multi-Agent。
- 不删除旧 Mini Runtime 和 SearchGoal smoke test。
